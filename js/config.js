(function() {

    var config = window.config = {
        URL: 'http://api.realtimeboard.com/objects/',
        debugURL: 'http://test.realtimeboard/',
        zoomLevel: 1,
        DEBUG: true
    }

    return config;

}())